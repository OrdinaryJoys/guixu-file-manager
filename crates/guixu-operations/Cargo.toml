[package]
name = "guixu-operations"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
guixu-domain = { path = "../guixu-domain" }
guixu-platform = { path = "../guixu-platform" }
guixu-storage = { path = "../guixu-storage" }
thiserror.workspace = true
blake3.workspace = true
libc.workspace = true
unicode-normalization.workspace = true

[features]
default = []
# 故障注入：在持久化节点等待 kill，供 kill-matrix 进程级测试使用（仅测试构建启用）。
fault-injection = []

[dev-dependencies]
tempfile = "3"
